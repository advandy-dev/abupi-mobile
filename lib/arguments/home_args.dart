import 'package:abupi/arguments/service_args.dart';

class HomeScreenArguments {
  final int? initialIndex;
  final ServiceScreenArguments? service;

  HomeScreenArguments({
    this.initialIndex,
    this.service
  });
}